"""Streamlit dashboard over the gold ``dash_*`` views (CLAUDE.md §4 end product).

The curated layer's non-technical consumer: reads the five pre-aggregated
BigQuery views live via ADC — the SAME gold layer the ML pipeline trains on, no
data or logic duplicated. See ``dashboard/README.md`` to run.
"""
